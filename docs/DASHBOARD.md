# Веб-панель статистики (Dashboard)

## 📊 Описание

Веб-панель предоставляет визуальную статистику бота в стиле Supabase:
- Тёмная тема с зелёными акцентами
- Реал-тайм статистика
- Минималистичный дизайн
- Защищена паролем

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

Панель использует `express` для веб-сервера.

### 2. Настройка переменных окружения

Добавьте в `.env`:

```env
# Веб-панель (опционально)
ENABLE_DASHBOARD=true
DASHBOARD_PORT=3000
DASHBOARD_PASSWORD=ваш_секретный_пароль
```

### 3. Запуск

```bash
npm start
```

Панель будет доступна по адресу: `http://localhost:3000`

## 🔐 Безопасность

### Аутентификация

- При первом входе требуется пароль
- Пароль задаётся в переменной `DASHBOARD_PASSWORD`
- По умолчанию: `admin123` (⚠️ обязательно измените!)

### Рекомендации

1. **Используйте сложный пароль:**
   ```env
   DASHBOARD_PASSWORD=My$ecureP@ssw0rd!2024
   ```

2. **Не публикуйте пароль:**
   - Файл `.env` должен быть в `.gitignore`
   - Не делитесь паролем в открытых каналах

3. **Используйте HTTPS:**
   - В продакшене настройте reverse proxy (nginx/caddy)
   - Добавьте SSL-сертификат (Let's Encrypt)

## 📈 Доступная статистика

### Общая информация
- 📊 Всего пользователей
- ✅ Активные сегодня
- 📅 Активные за неделю
- 📈 Процент выполнения заданий

### Топ пользователей
- 🔥 Топ-5 по текущим сериям
- Текущая и лучшая серии
- Статус активности

### Вопросы
- ❓ Всего вопросов в базе
- 💤 Неиспользованные вопросы
- 💡 Предложения от пользователей

## 🔧 API Endpoints

Все endpoints защищены аутентификацией `Bearer token`.

### GET `/api/stats/overview`
Общая статистика системы

**Response:**
```json
{
  "totalUsers": 150,
  "activeToday": 45,
  "activeWeek": 89,
  "activeMonth": 120,
  "totalCompleted": 3456,
  "totalMissed": 234,
  "completionRate": "93.7",
  "topStreaks": [...],
  "totalQuestions": 103,
  "pendingSuggestions": 12
}
```

### GET `/api/stats/questions`
Статистика по вопросам

**Response:**
```json
{
  "totalQuestions": 103,
  "mostPopular": [...],
  "leastPopular": [...],
  "unusedCount": 5
}
```

### GET `/api/users?sortBy=current_streak&order=DESC&limit=50`
Список пользователей с сортировкой

**Query Parameters:**
- `sortBy` - поле для сортировки (created_at, current_streak, etc.)
- `order` - порядок (ASC/DESC)
- `limit` - количество записей (по умолчанию 50)

### GET `/api/users/:telegramId`
Детальная информация о пользователе

### GET `/api/suggestions?limit=20`
Предложенные вопросы от пользователей

## 🎨 Кастомизация

### Изменение порта

В `.env`:
```env
DASHBOARD_PORT=8080
```

### Изменение цветовой схемы

Отредактируйте файл `web-dashboard.js`, секцию `<style>`:

```css
/* Основной цвет (зелёный) */
#3ecf8e → ваш цвет

/* Фон */
#0f0f0f → ваш цвет
```

### Добавление новых метрик

1. Создайте функцию в `database.js`
2. Добавьте API endpoint в `web-dashboard.js`
3. Обновите HTML и JavaScript для отображения

## 🚀 Деплой в продакшен

### Вариант 1: Отдельный порт

```bash
# На сервере
export ENABLE_DASHBOARD=true
export DASHBOARD_PORT=3000
export DASHBOARD_PASSWORD=секретный_пароль

npm start
```

Настройте firewall:
```bash
sudo ufw allow 3000/tcp
```

### Вариант 2: Через Nginx (рекомендуется)

Создайте конфиг nginx:

```nginx
server {
    listen 80;
    server_name stats.yourdomain.com;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

Добавьте SSL с Let's Encrypt:
```bash
sudo certbot --nginx -d stats.yourdomain.com
```

### Вариант 3: Docker

В `docker-compose.yml` добавьте порт:

```yaml
services:
  creativity-bot:
    ports:
      - "3000:3000"
    environment:
      - ENABLE_DASHBOARD=true
      - DASHBOARD_PORT=3000
      - DASHBOARD_PASSWORD=${DASHBOARD_PASSWORD}
```

## 📱 Автообновление

Статистика автоматически обновляется каждые 30 секунд.

Можно изменить интервал в коде:

```javascript
// В конце HTML
setInterval(() => {
  loadData();
}, 30000); // 30 секунд
```

## 🐛 Устранение неполадок

### Панель не запускается

**Проблема:** "Cannot find module 'express'"

**Решение:**
```bash
npm install express
```

---

**Проблема:** "EADDRINUSE: address already in use"

**Решение:**
Порт уже занят. Измените `DASHBOARD_PORT` или освободите порт:
```bash
lsof -ti:3000 | xargs kill -9
```

### Не могу войти

**Проблема:** "Unauthorized"

**Решение:**
1. Проверьте пароль в `.env`
2. Перезапустите бота
3. Проверьте консоль на ошибки

### Нет данных

**Проблема:** Панель загружается, но показывает нули

**Решение:**
1. Убедитесь, что база данных создана
2. Проверьте путь к БД в `database.js`
3. Добавьте тестовых пользователей через бота

## 📊 Примеры использования

### Мониторинг активности

Откройте панель утром и вечером, чтобы увидеть:
- Сколько пользователей выполнили задание
- Кто лидирует по сериям
- Сколько новых предложений

### Принятие решений

На основе статистики можно:
- Определить лучшее время для рассылок
- Найти проблемные вопросы
- Выявить самых активных пользователей

### Мотивация команды

Покажите панель инвесторам или партнёрам:
- Наглядная визуализация роста
- Показатели вовлечённости
- Актуальные метрики

## 🔮 Будущие улучшения

- [ ] Графики активности (Chart.js)
- [ ] Экспорт статистики в CSV/PDF
- [ ] Фильтры по датам
- [ ] Детальная аналитика по вопросам
- [ ] Настройки панели (тема, язык)
- [ ] Уведомления о критических событиях
- [ ] Мобильная версия (PWA)
- [ ] Множественные роли (админ, модератор, наблюдатель)

## 💡 Советы

1. **Регулярно проверяйте статистику** — хотя бы раз в день
2. **Следите за активностью** — падение может сигнализировать о проблеме
3. **Анализируйте топ пользователей** — они ваши амбассадоры
4. **Модерируйте предложения** — пользователи предлагают интересные идеи
5. **Делайте скриншоты** — для отчётов и презентаций

## 📞 Поддержка

Если возникли вопросы или проблемы:
- Проверьте логи: `docker compose logs -f`
- Откройте issue в репозитории
- Напишите автору: @kudinow

---

**Приятной работы с панелью! 📊✨**
