# 🚀 Инструкция: Как развернуть веб-панель на вашем сервере

Эта инструкция поможет вам добавить веб-панель статистики к боту, который уже работает на виртуальной машине в Яндекс.Облаке.

## Что вы получите

- Доступ к статистике бота через веб-браузер
- Красивый дашборд с реал-тайм данными
- Информация о пользователях, сериях, вопросах
- Защита паролем

---

## Шаг 1: Обновите код на локальном компьютере

### На вашем Mac:

```bash
cd "/Users/kudinow/Yandex.Disk.localized/Cursor/Бот для развитие креативности/projects/creativity-bot"

# Проверьте изменения
git status

# Добавьте все файлы
git add .

# Сделайте коммит
git commit -m "Добавлена поддержка веб-панели статистики"

# Загрузите на GitHub
git push origin main
```

---

## Шаг 2: Подключитесь к серверу

```bash
ssh ubuntu@ВАШ_IP_АДРЕС_СЕРВЕРА
```

Замените `ВАШ_IP_АДРЕС_СЕРВЕРА` на реальный IP вашей виртуальной машины.

---

## Шаг 3: Обновите код на сервере

```bash
cd ~/creativity-bot-rep

# Остановите бота
docker compose down

# Загрузите изменения
git pull origin main

# Должно появиться сообщение об обновлении файлов
```

---

## Шаг 4: Настройте веб-панель

Откройте файл `.env`:

```bash
nano .env
```

Добавьте в конец файла эти строки:

```env
# Веб-панель статистики
ENABLE_DASHBOARD=true
DASHBOARD_PORT=3000
DASHBOARD_PASSWORD=ВАШ_НАДЁЖНЫЙ_ПАРОЛЬ_123
```

**⚠️ Важно:** Замените `ВАШ_НАДЁЖНЫЙ_ПАРОЛЬ_123` на свой сложный пароль!

Пример хорошего пароля: `My$ecureD@shb0ard!2024`

Сохраните файл:
- Нажмите `Ctrl+O`
- Нажмите `Enter`
- Нажмите `Ctrl+X`

---

## Шаг 5: Откройте порт в файрволе

```bash
sudo ufw allow 3000/tcp
sudo ufw status
```

Вы должны увидеть в списке:

```
3000/tcp                   ALLOW       Anywhere
```

---

## Шаг 6: Запустите бота

```bash
docker compose up -d
```

---

## Шаг 7: Проверьте, что всё работает

Посмотрите логи:

```bash
docker compose logs | grep DASHBOARD
```

Вы должны увидеть:

```
[DASHBOARD] Веб-панель запущена на http://localhost:3000
[DASHBOARD] Пароль для входа: ВАШ_НАДЁЖНЫЙ_ПАРОЛЬ_123
```

---

## Шаг 8: Откройте панель в браузере

На вашем Mac откройте браузер и перейдите по адресу:

```
http://ВАШ_IP_АДРЕС_СЕРВЕРА:3000
```

Например: `http://51.250.12.34:3000`

Введите пароль, который вы указали в `.env` файле.

---

## ✅ Готово!

Теперь вы можете видеть статистику бота в реальном времени!

### Что показывает панель:

- 👥 Всего пользователей
- ✅ Активные сегодня
- 📅 Активные за неделю
- 📈 Процент выполнения заданий
- 🔥 Топ-5 пользователей по сериям
- ❓ Статистика по вопросам
- 💡 Предложения от пользователей

Панель обновляется автоматически каждые 30 секунд.

---

## 🔒 Сделать более безопасно (опционально)

Сейчас панель доступна по HTTP без шифрования. Для продакшена рекомендуется:

1. **Настроить SSL (HTTPS)**
2. **Использовать домен вместо IP**
3. **Установить Nginx как прокси**

Подробная инструкция в файле **[DASHBOARD_DEPLOY.md](./DASHBOARD_DEPLOY.md)**.

Для этого понадобится:
- Домен (например, `stats.yourdomain.ru`)
- 30-60 минут времени

Но это не обязательно — панель уже работает и защищена паролем!

---

## 🆘 Если что-то не работает

### Панель не открывается в браузере

```bash
# На сервере проверьте логи
docker compose logs -f

# Проверьте, что панель включена
cat .env | grep ENABLE_DASHBOARD

# Проверьте порт
sudo ufw status | grep 3000
```

### Ошибка "Unauthorized" при входе

Проверьте пароль:

```bash
cat .env | grep DASHBOARD_PASSWORD
```

Убедитесь, что вводите правильный пароль (без пробелов).

### Порт уже занят

Если порт 3000 занят, измените его:

```bash
nano .env
```

Поменяйте:
```env
DASHBOARD_PORT=8080
```

Перезапустите:
```bash
docker compose down
docker compose up -d
sudo ufw allow 8080/tcp
```

---

## 📱 Доступ с телефона

Панель адаптирована для мобильных устройств. Просто откройте ссылку на телефоне:

```
http://ВАШ_IP_АДРЕС_СЕРВЕРА:3000
```

---

## 🔄 Что делать при обновлении бота

В будущем, когда будете обновлять бота:

```bash
ssh ubuntu@ВАШ_IP_АДРЕС_СЕРВЕРА
cd ~/creativity-bot-rep
git pull origin main
docker compose down
docker compose up -d
```

Настройки панели сохранятся в `.env` файле.

---

## 📚 Дополнительная документация

- **[DASHBOARD_DEPLOY.md](./DASHBOARD_DEPLOY.md)** — полная инструкция с Nginx и SSL
- **[DASHBOARD.md](./DASHBOARD.md)** — описание всех возможностей панели
- **[DASHBOARD_QUICKSTART.md](./DASHBOARD_QUICKSTART.md)** — краткая справка

---

## 💡 Советы

1. **Сохраните ссылку в закладки** — для быстрого доступа
2. **Проверяйте статистику регулярно** — хотя бы раз в день
3. **Следите за активностью** — падение может сигнализировать о проблеме
4. **Анализируйте топ пользователей** — это ваши амбассадоры

---

**Готово! Приятной работы с панелью! 📊✨**

Если возникнут вопросы — пишите @kudinow
