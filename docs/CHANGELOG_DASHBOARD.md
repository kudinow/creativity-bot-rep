# История изменений: Веб-панель статистики

## [1.1.0] - 2024-12-17

### ✨ Добавлено

- **Поддержка удалённого доступа к веб-панели**
  - Веб-панель теперь слушает на `0.0.0.0` вместо `localhost`
  - Добавлен проброс портов в `docker-compose.yml`
  - Панель доступна извне контейнера

- **Документация для развёртывания на сервере**
  - `DASHBOARD_DEPLOY.md` — полная инструкция с Nginx и SSL
  - `DASHBOARD_QUICKSTART.md` — быстрый старт за 5 минут
  - `ИНСТРУКЦИЯ_ДЛЯ_СЕРВЕРА.md` — пошаговое руководство на русском

- **Улучшенная безопасность**
  - Инструкции по настройке SSL через Let's Encrypt
  - Конфигурация Nginx как reverse proxy
  - Рекомендации по ограничению доступа по IP

- **Обновлённая конфигурация**
  - Добавлены переменные окружения в `env.example`:
    - `ENABLE_DASHBOARD`
    - `DASHBOARD_PORT`
    - `DASHBOARD_PASSWORD`
    - `ADMIN_IDS`

### 🔧 Изменено

- **docker-compose.yml**
  - Добавлен проброс портов для веб-панели
  - Добавлены переменные окружения Dashboard

- **web-dashboard.js**
  - Изменён хост с `localhost` на `0.0.0.0`
  - Улучшены логи при запуске
  - Добавлены стили для ссылки на команды

- **DEPLOY.md**
  - Добавлен раздел о веб-панели
  - Ссылки на новую документацию
  - Обновлён раздел безопасности

- **README.md**
  - Добавлены ссылки на документацию Dashboard
  - Обновлён список файлов проекта

### 📚 Документация

Новые файлы:
- `DASHBOARD_DEPLOY.md` (полная инструкция, 600+ строк)
- `DASHBOARD_QUICKSTART.md` (быстрый старт)
- `ИНСТРУКЦИЯ_ДЛЯ_СЕРВЕРА.md` (пошаговое руководство)
- `CHANGELOG_DASHBOARD.md` (этот файл)

Обновлённые файлы:
- `docker-compose.yml`
- `env.example`
- `DEPLOY.md`
- `README.md`
- `web-dashboard.js`

### 🔐 Безопасность

- Подробные инструкции по настройке SSL/HTTPS
- Конфигурация Nginx с заголовками безопасности
- Рекомендации по сильным паролям
- Инструкции по настройке файрвола и Security Groups

### 💡 Использование

#### Локальный запуск (как раньше):
```bash
ENABLE_DASHBOARD=true npm start
# Доступ: http://localhost:3000
```

#### Запуск на сервере (новое):
```bash
# В .env:
ENABLE_DASHBOARD=true
DASHBOARD_PORT=3000
DASHBOARD_PASSWORD=ваш_пароль

docker compose up -d
# Доступ: http://ВАШ_IP:3000
```

#### С Nginx и SSL (рекомендуется):
```bash
# Настройка Nginx + SSL
sudo certbot --nginx -d stats.yourdomain.ru
# Доступ: https://stats.yourdomain.ru
```

### 🎯 Целевая аудитория

Эти изменения позволяют:
- Администраторам видеть статистику бота на удалённом сервере
- Получить доступ к панели с любого устройства
- Безопасно использовать панель через HTTPS
- Легко развернуть панель за 5 минут

### 🐛 Известные ограничения

- Панель не поддерживает множественные роли (только админ/пользователь)
- Нет графиков активности (только числовая статистика)
- Автообновление каждые 30 секунд (не настраивается)

### 📋 TODO в будущих версиях

- [ ] Добавить графики активности (Chart.js)
- [ ] Экспорт статистики в CSV/PDF
- [ ] Фильтры по датам
- [ ] Детальная аналитика по вопросам
- [ ] Настройки панели (интервал обновления, тема)
- [ ] Уведомления о критических событиях
- [ ] PWA для установки как приложение
- [ ] Множественные роли (админ, модератор, наблюдатель)

---

## Миграция с предыдущей версии

Если у вас уже был запущен бот:

### Шаг 1: Обновите код
```bash
git pull origin main
```

### Шаг 2: Добавьте переменные в .env
```env
ENABLE_DASHBOARD=true
DASHBOARD_PORT=3000
DASHBOARD_PASSWORD=ваш_пароль
```

### Шаг 3: Перезапустите бота
```bash
docker compose down
docker compose up -d
```

### Шаг 4: Откройте порт (если нужен внешний доступ)
```bash
sudo ufw allow 3000/tcp
```

---

## Благодарности

Спасибо всем, кто тестировал и предлагал улучшения!

---

## Поддержка

Если возникли вопросы:
- Проверьте [DASHBOARD_DEPLOY.md](./DASHBOARD_DEPLOY.md)
- Проверьте [DASHBOARD_QUICKSTART.md](./DASHBOARD_QUICKSTART.md)
- Создайте issue в репозитории
- Напишите автору: @kudinow
