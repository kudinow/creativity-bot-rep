# Руководство по резервному копированию и восстановлению

## Автоматическое резервное копирование

Бот автоматически создаёт резервные копии базы данных каждый день в 03:00 (МСК).

### Параметры автоматического бэкапа:
- ⏰ **Время:** 03:00 каждый день
- 📦 **Хранение:** последние 7 бэкапов
- 🗑️ **Очистка:** автоматическое удаление старых бэкапов
- 📁 **Папка:** `/backups` в корне проекта
- 📝 **Формат имени:** `database-backup-YYYY-MM-DD-HH-MM-SS.db`

### Что сохраняется:
- ✅ Все пользователи и их данные
- ✅ Статистика выполнения заданий
- ✅ Серии (текущие и лучшие)
- ✅ Бейджи и достижения
- ✅ История ответов
- ✅ Предложенные вопросы
- ✅ Все вопросы в базе

## Ручное создание бэкапа

### Через команду бота (рекомендуется)

```
/admin_backup
```

Бот создаст резервную копию и покажет информацию:
- Имя файла
- Размер
- Количество бэкапов

### Через скрипт

```bash
cd /path/to/creativity-bot
./scripts/backup-db.sh
```

### Вручную

```bash
cd /path/to/creativity-bot
cp database.db backups/database-backup-$(date +%Y-%m-%d-%H-%M-%S).db
```

## Просмотр списка бэкапов

### Через команду бота

```
/admin_backups
```

Покажет список всех бэкапов с:
- Датой и временем создания
- Размером файла
- Общим количеством

### Через терминал

```bash
cd /path/to/creativity-bot/backups
ls -lh
```

## Восстановление из бэкапа

### ⚠️ Важно перед восстановлением:

1. **Остановите бота** — иначе данные могут быть повреждены
2. **Создайте бэкап текущей БД** — на случай, если что-то пойдёт не так
3. **Убедитесь в правильности файла** — проверьте дату и размер

### Способ 1: Полное восстановление (рекомендуется)

```bash
# 1. Остановите бота
pm2 stop creativity-bot
# или
pkill -f "node src/index.js"

# 2. Создайте бэкап текущей БД (на всякий случай)
cp database.db database.db.before-restore

# 3. Восстановите из бэкапа
cp backups/database-backup-2025-12-29-03-00-00.db database.db

# 4. Проверьте права доступа
chmod 644 database.db

# 5. Запустите бота
pm2 start creativity-bot
# или
npm start
```

### Способ 2: Через скрипт

```bash
# Остановите бота
pm2 stop creativity-bot

# Восстановите из бэкапа
./scripts/restore-db.sh backups/database-backup-2025-12-29-03-00-00.db

# Запустите бота
pm2 start creativity-bot
```

### Способ 3: Восстановление на другом сервере

```bash
# 1. Скопируйте бэкап на новый сервер
scp backups/database-backup-2025-12-29-03-00-00.db user@new-server:/path/to/bot/

# 2. На новом сервере
cd /path/to/bot
mv database-backup-2025-12-29-03-00-00.db database.db
chmod 644 database.db

# 3. Запустите бота
npm start
```

## Проверка после восстановления

После восстановления проверьте:

1. **Бот запустился:**
```bash
pm2 status
# или
ps aux | grep "node src/index.js"
```

2. **Данные восстановлены:**
- Отправьте боту `/admin_stats`
- Проверьте количество пользователей
- Проверьте статистику

3. **Функции работают:**
- Отправьте `/start`
- Проверьте получение вопроса
- Попробуйте отправить ответ

## Частые проблемы

### Ошибка: "database is locked"

**Причина:** Бот всё ещё работает

**Решение:**
```bash
# Убедитесь, что бот остановлен
pm2 stop creativity-bot
pkill -9 -f "node src/index.js"

# Подождите 5 секунд
sleep 5

# Попробуйте снова
```

### Ошибка: "Permission denied"

**Причина:** Неправильные права доступа

**Решение:**
```bash
chmod 644 database.db
chown $USER:$USER database.db
```

### Бэкап не создаётся автоматически

**Проверьте:**

1. Бот запущен и работает
2. Планировщик запустился (в логах должно быть `[CRON] Планировщик запущен`)
3. Папка `/backups` существует и доступна для записи
4. Достаточно места на диске

**Создайте папку вручную:**
```bash
mkdir -p backups
chmod 755 backups
```

### Бэкапы занимают много места

**Решение:**

Автоматическая очистка уже настроена (хранятся только последние 7).

Если нужно удалить старые вручную:
```bash
cd backups
# Удалить бэкапы старше 7 дней
find . -name "database-backup-*.db" -mtime +7 -delete
```

## Рекомендации

### Для локальной разработки:
- ✅ Создавайте бэкап перед каждым большим изменением
- ✅ Используйте `/admin_backup` перед тестированием новых функций
- ✅ Храните важные бэкапы отдельно (скопируйте в другое место)

### Для production:
- ✅ Автоматические бэкапы уже настроены (03:00 каждый день)
- ✅ Дополнительно копируйте бэкапы в облако (Dropbox, Google Drive, S3)
- ✅ Проверяйте бэкапы раз в неделю (пробуйте восстановить)
- ✅ Храните бэкапы минимум 30 дней (измените в scheduler.js)

### Перед обновлением бота:
```bash
# 1. Создайте бэкап
/admin_backup

# 2. Скопируйте в безопасное место
cp backups/database-backup-*.db ~/safe-backups/

# 3. Обновляйте бот
git pull
npm install

# 4. Если что-то пошло не так - восстанавливайте
```

## Настройка хранения бэкапов

### Изменить количество хранимых бэкапов

Откройте `src/scheduler.js` и найдите строку:

```javascript
const keepCount = 7; // Измените на нужное количество
```

### Изменить время создания бэкапа

Откройте `src/scheduler.js` и найдите:

```javascript
cron.schedule('0 3 * * *', async () => {
  // Измените '0 3' на нужное время (часы минуты)
```

Примеры:
- `0 3` — 03:00
- `0 12` — 12:00 (полдень)
- `30 23` — 23:30 (перед закрытием дня)

### Копирование бэкапов в облако

Добавьте в cron задачу:

```bash
# Каждый день в 04:00 копировать в Dropbox
0 4 * * * rsync -av /path/to/bot/backups/ ~/Dropbox/bot-backups/
```

## Автоматизация

### Скрипт для копирования в облако

Создайте файл `scripts/backup-to-cloud.sh`:

```bash
#!/bin/bash

BACKUP_DIR="/path/to/bot/backups"
CLOUD_DIR="~/Dropbox/bot-backups"

# Копируем последний бэкап
LATEST_BACKUP=$(ls -t $BACKUP_DIR/database-backup-*.db | head -1)
cp "$LATEST_BACKUP" "$CLOUD_DIR/"

echo "Бэкап скопирован в облако: $LATEST_BACKUP"
```

Добавьте в crontab:
```bash
0 4 * * * /path/to/bot/scripts/backup-to-cloud.sh
```

## Мониторинг бэкапов

### Проверка последнего бэкапа

```bash
# Показать последний бэкап
ls -lht backups/ | head -2

# Проверить возраст последнего бэкапа
find backups/ -name "database-backup-*.db" -mtime -1
# Если пусто - бэкапы не создавались последние 24 часа
```

### Алерты при проблемах

Добавьте в `src/scheduler.js` после создания бэкапа:

```javascript
// Отправить уведомление админу
await bot.sendMessage(
  adminIds[0], 
  `✅ Автоматический бэкап создан: ${backupFileName}`
);
```

## Поддержка

Если возникли проблемы с бэкапами:

1. Проверьте логи бота
2. Убедитесь, что папка `/backups` существует
3. Проверьте права доступа
4. Создайте бэкап вручную через `/admin_backup`
5. Обратитесь к администратору: [@kudinow](https://t.me/kudinow)

