# 🔄 Как обновить бота (пошаговая инструкция)

Это руководство описывает процесс обновления бота на сервере после внесения изменений в код.

---

## 📋 **История обновлений**

### ✅ Обновление: Прогресс-бар (16.12.2025)

**Что добавлено:**
- Визуальный прогресс-бар с эмодзи (⬜ и ✅)
- Отображение прогресса после каждого ответа
- Улучшенные сообщения с информацией об оставшихся ответах

**Изменённые файлы:**
- `index.js` — добавлена функция `generateProgressBar()` и обновлены сообщения бота
- `README.md` — обновлено описание функциональности

**Как работает:**
- При `/start` показывается прогресс-бар текущего состояния
- После каждого ответа прогресс-бар обновляется: ⬜⬜⬜⬜⬜⬜⬜⬜⬜⬜ → ✅✅✅⬜⬜⬜⬜⬜⬜⬜
- При завершении все квадраты становятся зелёными: ✅✅✅✅✅✅✅✅✅✅

---

## 📝 **ЧАСТЬ 1: Внесение изменений локально (на Mac)**

### Шаг 1: Отредактируй код

Внеси нужные изменения в файлы проекта:
- `data/questions.js` — для добавления/изменения вопросов
- `database.js` — для изменения логики работы с БД
- `index.js` — для изменения логики бота
- и т.д.

### Шаг 2: Открой терминал в Cursor

Нажми `` Ctrl + ` `` или `Cmd + J`

### Шаг 3: Перейди в папку проекта

```bash
cd "/Users/kudinow/Yandex.Disk.localized/Cursor/Бот для развитие креативности/projects/creativity-bot"
```

### Шаг 4: Проверь статус изменений

```bash
git status
```

Ты увидишь список изменённых файлов.

### Шаг 5: Добавь изменения в Git

```bash
git add .
```

Или добавь конкретные файлы:
```bash
git add database.js data/questions.js
```

### Шаг 6: Создай коммит

```bash
git commit -m "Краткое описание изменений"
```

Например:
- `"Добавлено 50 новых вопросов"`
- `"Исправлена ошибка в подсчёте ответов"`
- `"Улучшена система напоминаний"`

### Шаг 7: Отправь изменения на GitHub

```bash
git push origin main
```

Если попросит авторизацию — введи свои данные GitHub.

---

## 🚀 **ЧАСТЬ 2: Обновление на сервере**

### Шаг 1: Подключись к серверу

```bash
ssh ubuntu@89.169.182.139
```

Введи пароль, когда попросит.

### Шаг 2: Перейди в папку проекта

```bash
cd ~/creativity-bot-rep
```

### Шаг 3: Получи обновления с GitHub

```bash
git pull origin main
```

Ты увидишь список обновлённых файлов:
```
Updating abc1234..def5678
Fast-forward
 database.js       | 10 ++++---
 data/questions.js | 95 ++++++++++++++++++++++++++++++++++++++++++++++++++++
 2 files changed, 100 insertions(+), 5 deletions(-)
```

### Шаг 4: Перезапусти бота

```bash
docker compose down
docker compose up -d
```

### Шаг 5: Проверь логи

```bash
docker compose logs -f
```

Должно появиться:
```
[БОТ] Инициализация...
[БД] Таблицы успешно созданы
[БД] Добавлено новых вопросов: X
[БД] Всего вопросов в базе: Y
[CRON] Планировщик запущен
[БОТ] Бот запущен и готов к работе
```

Нажми `Ctrl+C` чтобы выйти из просмотра логов.

### Шаг 6: Проверь статус бота

```bash
docker compose ps
```

Должно быть `Running` (Up).

### Шаг 7: Отключись от сервера

```bash
exit
```

---

## ✅ **Готово!**

Бот обновлён и работает с новыми изменениями!

---

## 📋 **Быстрая шпаргалка**

### На Mac:
```bash
cd "/Users/kudinow/Yandex.Disk.localized/Cursor/Бот для развитие креативности/projects/creativity-bot"
git add .
git commit -m "Описание изменений"
git push origin main
```

### На сервере:
```bash
ssh ubuntu@89.169.182.139
cd ~/creativity-bot-rep
git pull origin main
docker compose down
docker compose up -d
docker compose logs -f
exit
```

---

## 🚨 **Решение частых проблем**

### Проблема 1: `fatal: not a git repository`

**Решение:**
```bash
git init
git remote add origin https://github.com/kudinow/creativity-bot-rep.git
git fetch origin main
git reset --hard origin/main
```

---

### Проблема 2: `No such file or directory` при `cd ~/creativity-bot-rep`

**Решение:**

Проект не был склонирован. Выполни:
```bash
cd ~
git clone https://github.com/kudinow/creativity-bot-rep.git creativity-bot-rep
cp ~/.env ~/creativity-bot-rep/.env
cd ~/creativity-bot-rep
docker compose up -d
```

---

### Проблема 3: Конфликт при `git pull`

**Решение:**

Если на сервере были локальные изменения:
```bash
git stash
git pull origin main
git stash pop
```

Или просто сбрось локальные изменения:
```bash
git reset --hard origin/main
```

---

### Проблема 4: Docker контейнер не запускается

**Проверь логи:**
```bash
docker compose logs
```

**Перезапусти с пересборкой:**
```bash
docker compose down
docker compose up -d --build
```

---

### Проблема 5: База данных не обновляется

Если нужно **полностью пересоздать базу** (⚠️ удалит всех пользователей и статистику):

```bash
docker compose down
rm -f ~/creativity-bot-rep/data/database.db
docker compose up -d
```

---

## 🔐 **Важная информация**

### GitHub репозиторий:
```
https://github.com/kudinow/creativity-bot-rep
```

### IP адрес сервера:
```
89.169.182.139
```

### Путь к проекту на сервере:
```
/home/ubuntu/creativity-bot-rep
```

---

## 💡 **Полезные команды**

### Просмотр запущенных контейнеров:
```bash
docker ps
```

### Просмотр всех контейнеров (включая остановленные):
```bash
docker ps -a
```

### Просмотр логов без подключения к серверу:
```bash
ssh ubuntu@89.169.182.139 "cd ~/creativity-bot-rep && docker compose logs --tail=50"
```

### Перезапуск без остановки:
```bash
docker compose restart
```

### Полная очистка Docker (если что-то сломалось):
```bash
docker compose down
docker system prune -a
git pull origin main
docker compose up -d
```

---

## 📚 **Дополнительные ресурсы**

- [README.md](./README.md) — Общая документация проекта
- [DEPLOY.md](./DEPLOY.md) — Подробная инструкция по деплою с нуля
- [data/questions.js](./data/questions.js) — Файл с вопросами для редактирования

---

**Последнее обновление:** 16 декабря 2024
