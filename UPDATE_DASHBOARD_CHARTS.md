# 📊 Обновление Dashboard: Графики и модальные окна

## Что добавлено

### 1. Графики активности (2 шт)
- **График "Всего пользователей"** — линейный график роста пользователей за последние 30 дней
- **График "Активные сегодня"** — столбчатый график активности пользователей по дням

Графики расположены рядом друг с другом над секцией "Топ пользователей по сериям".

### 2. Модальные окна со списками вопросов
При клике на карточки открываются модальные окна:
- **"Всего вопросов"** → список всех вопросов с информацией об использовании
- **"Неиспользованных"** → список вопросов, которые ещё никто не получал
- **"Предложения пользователей"** → список предложенных вопросов с авторами и датами

---

## 📝 Изменённые файлы

1. **database.js** — добавлены 4 новые функции для получения данных
2. **web-dashboard.js** — добавлены API endpoints, графики, модальные окна и стили

---

## 🚀 Как обновить на сервере

### Шаг 1: На Mac (загрузите изменения на GitHub)

```bash
cd "/Users/kudinow/Yandex.Disk.localized/Cursor/Бот для развитие креативности/projects/creativity-bot"

# Проверьте изменения
git status

# Добавьте файлы
git add database.js web-dashboard.js UPDATE_DASHBOARD_CHARTS.md

# Создайте коммит
git commit -m "Добавлены графики и модальные окна в Dashboard"

# Загрузите на GitHub
git push origin main
```

### Шаг 2: На сервере (обновите код)

```bash
# Подключитесь к серверу
ssh ubuntu@89.169.182.139

# Перейдите в директорию проекта
cd ~/creativity-bot-rep

# Остановите бота
docker compose down

# Загрузите изменения
git pull origin main

# Запустите бота
docker compose up -d

# Проверьте логи
docker compose logs -f
```

### Шаг 3: Откройте обновлённую панель

```
http://89.169.182.139:3000
```

Обновите страницу (Ctrl+R или Cmd+R) чтобы увидеть изменения.

---

## ✨ Что вы увидите

### Графики

**Над секцией "Топ пользователей":**

```
┌─────────────────────────┐  ┌─────────────────────────┐
│ 📈 Всего пользователей  │  │ ✅ Активные сегодня     │
│                         │  │                         │
│   [Линейный график]     │  │   [Столбчатый график]   │
│                         │  │                         │
└─────────────────────────┘  └─────────────────────────┘
```

**Графики показывают:**
- Последние 30 дней
- Автоматически обновляются каждые 30 секунд
- Тёмная тема с зелёными акцентами (в стиле Supabase)

### Кликабельные карточки

Карточки "Всего вопросов", "Неиспользованных" и "Предложения пользователей" теперь:
- Имеют подсказку "Нажмите для просмотра"
- Подсвечиваются при наведении
- При клике открывают модальное окно со списком

### Модальные окна

**Формат отображения:**

**Все вопросы:**
```
1. 10 способов решить проблему без денег
   👥 Использовали: 5 чел. | 📊 Всего использований: 12
```

**Неиспользованные:**
```
1. 10 способов использовать старую футболку
   💤 Ещё не использовался
```

**Предложения:**
```
1. 10 идей для выходного дня
   👤 От: 123456789 | 📅 Дата: 17.12.2024
```

---

## 🔍 Технические детали

### Новые функции в database.js

1. **getUsersStatsByDays(days)** — статистика пользователей по дням
   - Возвращает массив объектов: `{date, totalUsers, activeUsers}`
   - По умолчанию за 30 дней

2. **getAllQuestionsWithUsage()** — все вопросы с информацией об использовании
   - Возвращает: `{id, text, used_by_users, total_usage}`

3. **getUnusedQuestions()** — неиспользованные вопросы
   - Возвращает вопросы, которые никогда не попадали в daily_progress

4. **getSuggestedQuestionsWithDetails()** — предложенные вопросы с деталями
   - Возвращает: `{id, question_text, created_at, telegram_id, status}`

### Новые API endpoints

```
GET /api/stats/daily?days=30       - Статистика по дням
GET /api/questions/all             - Все вопросы
GET /api/questions/unused          - Неиспользованные
GET /api/questions/suggested       - Предложенные
```

Все endpoints требуют авторизации через Bearer token.

### Используемые библиотеки

- **Chart.js 4.4.0** — для графиков (загружается через CDN)
- **Vanilla JavaScript** — для модальных окон (без дополнительных зависимостей)

---

## 🎨 Дизайн

### Цветовая схема

- **Основной цвет:** #3ecf8e (зелёный)
- **Фон:** #0f0f0f, #1a1a1a (тёмные оттенки)
- **Границы:** #2d2d2d
- **Текст:** #ededed (светлый), #9ca3af (серый)

### Адаптивность

- Графики адаптируются под ширину экрана
- На мобильных устройствах графики располагаются вертикально
- Модальные окна занимают 90% ширины экрана (max 800px)

---

## 🔧 Настройка (опционально)

### Изменить период графиков

В файле `web-dashboard.js` найдите строку:

```javascript
fetch('/api/stats/daily?days=30', {
```

Измените `days=30` на нужное значение (например, `days=7` для недели).

### Изменить цвета графиков

В функции `loadCharts()` найдите:

```javascript
borderColor: '#3ecf8e',
backgroundColor: 'rgba(62, 207, 142, 0.1)',
```

Замените на свои цвета.

### Изменить высоту графиков

Добавьте в стили `.chart-card canvas`:

```css
.chart-card canvas {
  max-height: 300px;
}
```

---

## 📊 Примеры данных

### API Response: /api/stats/daily

```json
[
  {
    "date": "2024-12-01",
    "totalUsers": 45,
    "activeUsers": 23
  },
  {
    "date": "2024-12-02",
    "totalUsers": 47,
    "activeUsers": 25
  }
]
```

### API Response: /api/questions/all

```json
[
  {
    "id": 1,
    "text": "10 способов решить проблему без денег",
    "used_by_users": 5,
    "total_usage": 12
  }
]
```

---

## 🆘 Устранение проблем

### Графики не отображаются

**Проблема:** Видны только заголовки, но нет графиков

**Решения:**

1. Проверьте консоль браузера (F12) на ошибки
2. Убедитесь, что Chart.js загрузился:
   ```javascript
   console.log(typeof Chart); // должно быть "function"
   ```
3. Проверьте, что данные приходят:
   ```bash
   curl -H "Authorization: Bearer ваш_пароль" \
     http://89.169.182.139:3000/api/stats/daily?days=30
   ```

### Модальные окна не открываются

**Проблема:** Клик по карточке не работает

**Решения:**

1. Проверьте консоль браузера на ошибки JavaScript
2. Убедитесь, что функция `showQuestionsModal` определена
3. Проверьте, что API endpoints отвечают:
   ```bash
   curl -H "Authorization: Bearer ваш_пароль" \
     http://89.169.182.139:3000/api/questions/all
   ```

### Ошибка "Internal server error"

**Проблема:** API возвращает 500 ошибку

**Решения:**

1. Проверьте логи сервера:
   ```bash
   docker compose logs | grep ERROR
   ```

2. Убедитесь, что новые функции добавлены в database.js:
   ```bash
   grep "getUsersStatsByDays" database.js
   ```

3. Перезапустите бота:
   ```bash
   docker compose restart
   ```

---

## 💡 Дополнительные возможности

### Экспорт данных

Можете добавить кнопку экспорта в модальном окне:

```javascript
function exportToCSV(data) {
  const csv = data.map(item => `"${item.text}"`).join('\n');
  const blob = new Blob([csv], { type: 'text/csv' });
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = 'questions.csv';
  a.click();
}
```

### Фильтрация вопросов

Добавьте поле поиска в модальное окно:

```html
<input type="text" id="searchInput" placeholder="Поиск...">
```

### Дополнительные графики

Можете добавить:
- График процента выполнения по дням
- График новых пользователей по дням
- Тепловая карта активности по дням недели

---

## 📚 Связанные документы

- **DASHBOARD.md** — полная документация по веб-панели
- **DASHBOARD_DEPLOY.md** — развёртывание на сервере
- **CHANGELOG.md** — история изменений

---

**Готово! Dashboard обновлён! 📊✨**

Теперь у вас есть наглядные графики активности и удобный просмотр списков вопросов.
