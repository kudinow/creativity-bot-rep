# Скрипты для управления ботом

Утилиты для автоматизации деплоя и управления ботом в облаке.

## Доступные скрипты

### 0. migrate-streaks.js

**Описание:** Скрипт миграции для добавления системы стриков в базу данных.

**Что делает:**
- Добавляет поля `current_streak`, `best_streak`, `last_completed_date` в таблицу `users`
- Создаёт таблицы `badges` и `user_badges`
- Загружает 4 бейджа (🔥 Новичок, 🌟 Энтузиаст, 💎 Мастер, 👑 Легенда)

**Использование:**

```bash
# Локально
node scripts/migrate-streaks.js

# На сервере через Docker
docker compose exec creativity-bot node scripts/migrate-streaks.js
```

**Когда использовать:**
- После обновления кода с новой системой стриков
- Если команды `/stats` или `/streak` не работают
- Если в логах есть ошибки о несуществующих полях БД

---

### 1. deploy-prepare.sh

Подготовка проекта к деплою на сервер.

**Что делает:**
- Проверяет наличие и корректность `.env` файла
- Создаёт архив проекта со всеми необходимыми файлами
- Исключает локальные файлы (node_modules, database.db, .env)
- Создаёт `.env.server` с шаблоном для сервера

**Использование:**
```bash
./scripts/deploy-prepare.sh
```

**Результат:** Файл `creativity-bot-deploy.tar.gz` готовый к загрузке на сервер.

---

### 2. backup-db.sh

Резервное копирование базы данных с сервера.

**Что делает:**
- Скачивает базу данных с удалённого сервера
- Сохраняет в директорию `backups/` с датой и временем
- Автоматически удаляет старые бэкапы (оставляет 10 последних)
- Работает как с Docker volume, так и с локальными файлами

**Использование:**
```bash
./scripts/backup-db.sh <IP-адрес-сервера>

# Пример:
./scripts/backup-db.sh 51.250.12.34
```

**Результат:** Файл `backups/database_YYYYMMDD_HHMMSS.db`

---

### 3. restore-db.sh

Восстановление базы данных на сервер из бэкапа.

**Что делает:**
- Останавливает бота на сервере
- Загружает файл бэкапа на сервер
- Заменяет текущую базу данных
- Запускает бота
- Показывает логи для проверки

**Использование:**
```bash
./scripts/restore-db.sh <IP-адрес-сервера> <файл-бэкапа>

# Пример:
./scripts/restore-db.sh 51.250.12.34 backups/database_20231215_120000.db
```

**Внимание:** Текущая база на сервере будет полностью заменена!

---

## Типичный рабочий процесс

### Первоначальный деплой

1. Подготовьте проект:
   ```bash
   ./scripts/deploy-prepare.sh
   ```

2. Загрузите на сервер:
   ```bash
   scp creativity-bot-deploy.tar.gz yc-user@<IP>:~/
   ```

3. На сервере:
   ```bash
   mkdir -p ~/creativity-bot
   tar -xzf creativity-bot-deploy.tar.gz -C ~/creativity-bot
   cd ~/creativity-bot
   cp .env.server .env
   nano .env  # добавьте реальный токен
   docker-compose up -d
   ```

### Регулярное резервное копирование

Рекомендуется делать бэкапы еженедельно или перед важными изменениями:

```bash
# Создание бэкапа
./scripts/backup-db.sh 51.250.12.34

# Бэкапы сохраняются в backups/
ls -lh backups/
```

### Восстановление после сбоя

Если что-то пошло не так:

```bash
# Посмотрите доступные бэкапы
ls -lh backups/

# Восстановите нужный
./scripts/restore-db.sh 51.250.12.34 backups/database_20231215_120000.db
```

### Обновление кода

После изменения кода:

```bash
# 1. Сделайте бэкап (на всякий случай)
./scripts/backup-db.sh 51.250.12.34

# 2. Подготовьте новую версию
./scripts/deploy-prepare.sh

# 3. Загрузите на сервер
scp creativity-bot-deploy.tar.gz yc-user@<IP>:~/

# 4. На сервере:
ssh yc-user@<IP>
cd ~/creativity-bot
docker-compose down
tar -xzf ~/creativity-bot-deploy.tar.gz -C ~/creativity-bot
docker-compose build --no-cache
docker-compose up -d
```

---

## Автоматизация бэкапов

Можно настроить автоматическое ежедневное резервное копирование через cron на вашем локальном компьютере:

```bash
# Откройте crontab
crontab -e

# Добавьте строку (бэкап каждый день в 2:00 ночи)
0 2 * * * cd /Users/kudinow/Yandex.Disk.localized/Cursor/Бот\ для\ развитие\ креативности/projects/creativity-bot && ./scripts/backup-db.sh 51.250.12.34 >> logs/backup.log 2>&1
```

Или через GitHub Actions, если проект в репозитории.

---

## Требования

- **SSH доступ** к серверу
- **SSH ключ** настроен для подключения без пароля
- **Пользователь:** `yc-user` (по умолчанию для Яндекс Облака)
- **Директория бота:** `~/creativity-bot` на сервере

Если у вас другой пользователь или директория, отредактируйте скрипты соответственно.

---

## Устранение проблем

### Permission denied

```bash
chmod +x scripts/*.sh
```

### SSH ключ не работает

```bash
# Проверьте, что ключ добавлен на сервер
cat ~/.ssh/id_rsa.pub

# На сервере должен быть в
~/.ssh/authorized_keys
```

### Скрипт не находит файлы

Убедитесь, что запускаете скрипты из корневой директории проекта:

```bash
cd /Users/kudinow/Yandex.Disk.localized/Cursor/Бот\ для\ развитие\ креативности/projects/creativity-bot
./scripts/deploy-prepare.sh
```

---

## Дополнительно

Все скрипты используют цветной вывод для лучшей читаемости:
- 🟢 Зелёный — успех
- 🟡 Жёлтый — предупреждение
- 🔴 Красный — ошибка

Скрипты включают проверки безопасности и подтверждения для критических операций.
