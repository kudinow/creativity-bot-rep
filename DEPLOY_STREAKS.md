# 🚀 Деплой системы стриков на сервер

## ⚠️ Проблема: Изменения не доходят до сервера

Если вы сделали коммиты локально, но сервер их не видит — нужно **загрузить изменения на GitHub**.

---

## ✅ Решение (пошагово)

### Шаг 1: Проверьте статус на Mac

```bash
cd "/Users/kudinow/Yandex.Disk.localized/Cursor/Бот для развитие креативности/projects/creativity-bot"
git status
```

Вы увидите:
- Изменённые файлы (modified)
- Новые файлы (untracked)
- Файлы готовые к коммиту (staged)

### Шаг 2: Добавьте все изменения

```bash
git add .
```

Эта команда добавляет все файлы в staging area.

### Шаг 3: Создайте коммит

```bash
git commit -m "Добавлена система стриков и бейджей"
```

### Шаг 4: **ВАЖНО!** Загрузите на GitHub

```bash
git push origin main
```

Или если ваша ветка называется `master`:
```bash
git push origin master
```

**Именно этот шаг часто пропускают!** Без `git push` изменения остаются только на вашем компьютере.

### Шаг 5: Проверьте на GitHub

Откройте репозиторий на GitHub в браузере и убедитесь, что:
- Последний коммит виден
- Новые файлы присутствуют (scripts/migrate-streaks.js, MIGRATION_GUIDE.md, и т.д.)

---

## 🌐 Применение изменений на сервере

После того как вы сделали `git push`, подключитесь к серверу:

### Шаг 1: Подключитесь к серверу

```bash
ssh ubuntu@ВАШ_IP_АДРЕС
```

### Шаг 2: Перейдите в папку проекта

```bash
cd ~/creativity-bot-repo
```

Или если у вас другое название папки:
```bash
cd ~/creativity-bot-rep
```

### Шаг 3: Проверьте текущее состояние

```bash
git status
git log --oneline -5
```

### Шаг 4: Получите изменения с GitHub

```bash
git pull origin main
```

Вы должны увидеть список загруженных файлов.

### Шаг 5: Пересоберите и перезапустите Docker

```bash
# Создайте бэкап БД (на всякий случай)
./scripts/backup-db.sh

# Остановите бота
docker compose down

# Пересоберите образ с новым кодом
docker compose build --no-cache

# Запустите бота
docker compose up -d

# Смотрите логи
docker compose logs -f
```

### Шаг 6: Проверьте логи

В логах должно появиться:
```
[БОТ] Инициализация...
[БД] Таблицы успешно созданы
[БД] Добавлено поле current_streak в таблицу users
[БД] Добавлено поле best_streak в таблицу users
[БД] Добавлено поле last_completed_date в таблицу users
[БД] Добавлено новых бейджей: 4
[БД] Всего бейджей в базе: 4
[CRON] Планировщик запущен
[БОТ] Бот запущен и готов к работе
```

Нажмите `Ctrl+C` чтобы выйти из просмотра логов.

---

## 🧪 Тестирование

Откройте бота в Telegram и проверьте команды:

### 1. Команда `/stats`

Должна показывать:
```
📊 Твоя статистика:

✅ Выполнено дней: X
❌ Пропущено дней: Y

🔥 Текущая серия: X дней
🏆 Лучшая серия: X дней

Используй /streak для подробностей о сериях и бейджах
```

### 2. Команда `/streak`

Должна показывать:
```
🔥 Твои серии:

📈 Текущая серия: X дней
🏆 Лучшая серия: X дней

🎖 Твои бейджи:
У тебя пока нет бейджей

🎯 Следующие цели:
🔥 Новичок — 3 дня подряд (ещё 3 дня)
🌟 Энтузиаст — 7 дней подряд
💎 Мастер — 30 дней подряд
👑 Легенда — 100 дней подряд
```

### 3. Выполните задание

После выполнения задания (10 ответов) должно появиться:
```
✅ Отлично! Ты выполнил задание на сегодня!
🔥 Серия: 1 день

Завтра будет новый вопрос.
```

---

## 🆘 Если что-то пошло не так

### Проблема 1: `git pull` говорит "Already up to date"

**Причина:** Вы не сделали `git push` на Mac.

**Решение:**
1. Выйдите с сервера: `exit`
2. На Mac выполните: `git push origin main`
3. Вернитесь на сервер и повторите `git pull`

### Проблема 2: Ошибки SQL в логах

**Причина:** Миграция не применилась автоматически.

**Решение:**
```bash
docker compose exec creativity-bot node scripts/migrate-streaks.js
docker compose restart
docker compose logs -f
```

### Проблема 3: Файл migrate-streaks.js не найден

**Причина:** Файл не загружен на GitHub или `git pull` не выполнен.

**Решение:**
1. Проверьте на GitHub наличие файла
2. Если файла нет — сделайте `git push` на Mac
3. Если файл есть — на сервере выполните `git pull origin main`

### Проблема 4: Бейджи не появляются

**Причина:** Нужно выполнить задание минимум 3 дня подряд.

**Решение:** Это нормально! Бейджи появятся после выполнения заданий.

---

## 📝 Чеклист для следующих обновлений

### На Mac (каждый раз):
- [ ] `git add .`
- [ ] `git commit -m "Описание"`
- [ ] **`git push origin main`** ⚠️ НЕ ЗАБУДЬТЕ!
- [ ] Проверьте на GitHub что коммит виден

### На сервере:
- [ ] `ssh ubuntu@IP`
- [ ] `cd ~/creativity-bot-repo`
- [ ] `./scripts/backup-db.sh` (для безопасности)
- [ ] `git pull origin main`
- [ ] `docker compose down`
- [ ] `docker compose build --no-cache` (если менялся код)
- [ ] `docker compose up -d`
- [ ] `docker compose logs -f` (проверка)
- [ ] Тест команд в Telegram

---

## 🎉 Готово!

После выполнения всех шагов система стриков будет работать!

Пользователи смогут:
- ✅ Видеть свою серию в `/stats`
- ✅ Получать бейджи за достижения
- ✅ Отслеживать свой рекорд
- ✅ Видеть прогресс до следующего бейджа

---

## 📚 Полезные ссылки

- [QUICK_UPDATE.md](./QUICK_UPDATE.md) — краткая шпаргалка
- [MIGRATION_GUIDE.md](./MIGRATION_GUIDE.md) — про миграции БД
- [HOW_TO_UPDATE.md](./HOW_TO_UPDATE.md) — полная инструкция

