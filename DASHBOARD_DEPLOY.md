# Развёртывание веб-панели статистики на удалённом сервере

Это руководство поможет настроить веб-панель статистики для бота, работающего на виртуальной машине в Яндекс.Облаке.

## 📋 Содержание

1. [Быстрый старт](#быстрый-старт)
2. [Подробная настройка](#подробная-настройка)
3. [Безопасность с Nginx](#безопасность-с-nginx)
4. [Использование](#использование)
5. [Устранение проблем](#устранение-проблем)

---

## 🚀 Быстрый старт

### Вариант 1: Прямой доступ через IP (для тестирования)

Этот способ подходит для быстрого тестирования, но **не рекомендуется для продакшена**.

#### Шаг 1: Подключитесь к серверу

```bash
ssh ubuntu@ВАШ_IP_АДРЕС
cd ~/creativity-bot-rep
```

#### Шаг 2: Отредактируйте .env файл

```bash
nano .env
```

Добавьте следующие строки:

```env
# Веб-панель статистики
ENABLE_DASHBOARD=true
DASHBOARD_PORT=3000
DASHBOARD_PASSWORD=ваш_супер_надёжный_пароль_123
```

**⚠️ Важно:** Используйте сложный пароль! Это единственная защита панели.

Сохраните файл: `Ctrl+O` → Enter → `Ctrl+X`

#### Шаг 3: Откройте порт в файрволе

```bash
# Если используете UFW
sudo ufw allow 3000/tcp
sudo ufw status

# Или через Яндекс.Облако Security Groups
# См. раздел "Настройка Security Groups" ниже
```

#### Шаг 4: Перезапустите бота

```bash
docker compose down
docker compose up -d
```

#### Шаг 5: Проверьте логи

```bash
docker compose logs -f
```

Вы должны увидеть:

```
[БОТ] Бот запущен и готов к работе
[DASHBOARD] Веб-панель запущена на http://localhost:3000
[DASHBOARD] Пароль для входа: ваш_супер_надёжный_пароль_123
```

#### Шаг 6: Откройте панель в браузере

```
http://ВАШ_IP_АДРЕС:3000
```

Введите пароль из `.env` файла.

---

## 🔒 Вариант 2: Безопасный доступ через Nginx (рекомендуется)

Этот способ добавляет SSL-шифрование и правильную настройку безопасности.

### Предварительные требования

- Домен (например, `stats.yourdomain.ru`)
- DNS запись типа A, указывающая на IP вашего сервера

### Шаг 1: Установите Nginx

```bash
sudo apt update
sudo apt install nginx -y
```

### Шаг 2: Настройте бота

Отредактируйте `.env`:

```bash
cd ~/creativity-bot-rep
nano .env
```

Добавьте:

```env
ENABLE_DASHBOARD=true
DASHBOARD_PORT=3000
DASHBOARD_PASSWORD=ваш_супер_надёжный_пароль
```

**Важно:** Порт 3000 будет доступен только локально внутри сервера.

### Шаг 3: Перезапустите бота

```bash
docker compose down
docker compose up -d
```

### Шаг 4: Создайте конфигурацию Nginx

```bash
sudo nano /etc/nginx/sites-available/creativity-bot-dashboard
```

Вставьте следующий конфиг:

```nginx
server {
    listen 80;
    server_name stats.yourdomain.ru;  # Замените на ваш домен

    # Ограничение размера запроса
    client_max_body_size 10M;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        
        # WebSocket поддержка (на случай будущих обновлений)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        
        # Заголовки
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        
        proxy_cache_bypass $http_upgrade;
        
        # Таймауты
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Безопасность
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}
```

Сохраните: `Ctrl+O` → Enter → `Ctrl+X`

### Шаг 5: Активируйте конфигурацию

```bash
# Создайте символическую ссылку
sudo ln -s /etc/nginx/sites-available/creativity-bot-dashboard /etc/nginx/sites-enabled/

# Проверьте конфигурацию
sudo nginx -t

# Перезапустите Nginx
sudo systemctl restart nginx
```

### Шаг 6: Установите SSL-сертификат (Let's Encrypt)

```bash
# Установите Certbot
sudo apt install certbot python3-certbot-nginx -y

# Получите сертификат (автоматически обновит конфиг Nginx)
sudo certbot --nginx -d stats.yourdomain.ru

# Следуйте инструкциям Certbot
# Выберите "Redirect HTTP to HTTPS" (вариант 2)
```

Certbot автоматически:
- Получит SSL-сертификат
- Обновит конфигурацию Nginx
- Настроит автообновление сертификата

### Шаг 7: Настройте файрвол

```bash
# Закройте прямой доступ к порту 3000 (если открывали)
sudo ufw delete allow 3000/tcp

# Откройте порты для Nginx
sudo ufw allow 'Nginx Full'
sudo ufw status
```

Должно быть:
```
80/tcp                     ALLOW       Anywhere
443/tcp                    ALLOW       Anywhere
```

### Шаг 8: Откройте панель

Теперь панель доступна по адресу:

```
https://stats.yourdomain.ru
```

✅ С SSL-шифрованием  
✅ С защитой паролем  
✅ Без прямого доступа к портам

---

## ⚙️ Подробная настройка

### Настройка Security Groups в Яндекс.Облаке

Если вы используете Security Groups (группы безопасности):

1. Откройте [Yandex Cloud Console](https://console.cloud.yandex.ru)
2. Перейдите в **Compute Cloud** → **Виртуальные машины**
3. Выберите вашу VM `creativity-bot`
4. Перейдите на вкладку **Группы безопасности**

#### Для варианта 1 (прямой доступ):

Добавьте правило:
- **Направление:** Входящий трафик
- **Протокол:** TCP
- **Диапазон портов:** 3000
- **CIDR:** `ваш_ip/32` (только ваш IP) или `0.0.0.0/0` (любой IP, не рекомендуется)

#### Для варианта 2 (Nginx):

Добавьте правила:
- **HTTP:**
  - Направление: Входящий
  - Протокол: TCP
  - Порт: 80
  - CIDR: `0.0.0.0/0`

- **HTTPS:**
  - Направление: Входящий
  - Протокол: TCP
  - Порт: 443
  - CIDR: `0.0.0.0/0`

### Изменение порта

Если порт 3000 занят:

1. Измените в `.env`:
   ```env
   DASHBOARD_PORT=8080
   ```

2. Обновите `docker-compose.yml` (если нужно):
   ```yaml
   ports:
     - "8080:8080"
   ```

3. Обновите конфигурацию Nginx:
   ```nginx
   proxy_pass http://localhost:8080;
   ```

4. Перезапустите всё:
   ```bash
   docker compose down
   docker compose up -d
   sudo systemctl restart nginx
   ```

### Ограничение доступа по IP (дополнительная защита)

Добавьте в конфиг Nginx:

```nginx
location / {
    # Разрешить только ваш IP
    allow ВАШ_IP_АДРЕС;
    deny all;
    
    proxy_pass http://localhost:3000;
    # ... остальные настройки
}
```

---

## 📊 Использование

### Первый вход

1. Откройте панель в браузере
2. Введите пароль из переменной `DASHBOARD_PASSWORD`
3. Пароль сохранится в браузере (до закрытия вкладки)

### Что показывает панель

#### Основная статистика:
- 👥 Всего пользователей
- ✅ Активные сегодня
- 📅 Активные за неделю
- 📈 Процент выполнения заданий

#### Топ пользователей:
- 🔥 Топ-5 по текущим сериям
- Текущая и лучшая серии
- Статус активности

#### Вопросы:
- ❓ Всего вопросов в базе
- 💤 Неиспользованные вопросы
- 💡 Предложения от пользователей

#### Дополнительно:
- 📋 Справка по административным командам (`/commands`)

### Автообновление

Статистика обновляется автоматически каждые 30 секунд. Не нужно перезагружать страницу.

### API для интеграций

Если хотите получать данные программно:

```bash
# Общая статистика
curl -H "Authorization: Bearer ваш_пароль" \
  https://stats.yourdomain.ru/api/stats/overview

# Список пользователей
curl -H "Authorization: Bearer ваш_пароль" \
  https://stats.yourdomain.ru/api/users

# Статистика вопросов
curl -H "Authorization: Bearer ваш_пароль" \
  https://stats.yourdomain.ru/api/stats/questions
```

---

## 🔐 Безопасность

### Рекомендации по паролю

✅ **Хорошие пароли:**
```
My$ecureD@shb0ard!2024
Cr3@tiv1ty-B0t-$tats#2024
P@ssw0rd!Dashboard#Secure
```

❌ **Плохие пароли:**
```
admin123
password
12345678
```

### Смена пароля

1. Отредактируйте `.env`:
   ```bash
   nano .env
   ```

2. Измените `DASHBOARD_PASSWORD`

3. Перезапустите бота:
   ```bash
   docker compose restart
   ```

4. Обновите страницу в браузере и войдите с новым паролем

### Автоматическое обновление SSL-сертификата

Certbot настраивает автообновление. Проверить можно так:

```bash
# Проверка статуса таймера
sudo systemctl status certbot.timer

# Тестовое обновление
sudo certbot renew --dry-run
```

### Мониторинг доступа

Просмотр логов Nginx:

```bash
# Последние 100 запросов
sudo tail -n 100 /var/log/nginx/access.log

# Ошибки
sudo tail -n 100 /var/log/nginx/error.log

# В реальном времени
sudo tail -f /var/log/nginx/access.log
```

---

## 🛠 Устранение проблем

### Панель не открывается

**Проблема:** `ERR_CONNECTION_REFUSED` или `This site can't be reached`

**Решения:**

1. Проверьте, запущен ли бот:
   ```bash
   docker compose ps
   ```

2. Проверьте логи:
   ```bash
   docker compose logs | grep DASHBOARD
   ```

3. Проверьте, что панель включена:
   ```bash
   cat .env | grep ENABLE_DASHBOARD
   ```
   Должно быть `ENABLE_DASHBOARD=true`

4. Проверьте порты:
   ```bash
   sudo netstat -tlnp | grep 3000
   ```

5. Проверьте файрвол:
   ```bash
   sudo ufw status
   ```

### Не могу войти (Unauthorized)

**Проблема:** Панель открывается, но выдаёт ошибку при входе

**Решения:**

1. Проверьте пароль в `.env`:
   ```bash
   cat .env | grep DASHBOARD_PASSWORD
   ```

2. Убедитесь, что нет лишних пробелов:
   ```env
   # Правильно:
   DASHBOARD_PASSWORD=mypassword123
   
   # Неправильно:
   DASHBOARD_PASSWORD= mypassword123
   DASHBOARD_PASSWORD=mypassword123 
   ```

3. Перезапустите бота после изменения `.env`

### Nginx выдаёт 502 Bad Gateway

**Проблема:** Nginx работает, но не может подключиться к боту

**Решения:**

1. Проверьте, запущен ли бот:
   ```bash
   docker compose ps
   docker compose logs
   ```

2. Проверьте порт в конфиге Nginx:
   ```bash
   sudo cat /etc/nginx/sites-available/creativity-bot-dashboard | grep proxy_pass
   ```
   Должно быть: `proxy_pass http://localhost:3000;`

3. Проверьте, что порт совпадает в `.env`:
   ```bash
   cat .env | grep DASHBOARD_PORT
   ```

4. Перезапустите всё:
   ```bash
   docker compose restart
   sudo systemctl restart nginx
   ```

### SSL-сертификат не устанавливается

**Проблема:** Certbot выдаёт ошибку

**Решения:**

1. Проверьте DNS:
   ```bash
   nslookup stats.yourdomain.ru
   ```
   Должен вернуть IP вашего сервера

2. Проверьте, что домен доступен:
   ```bash
   curl http://stats.yourdomain.ru
   ```

3. Проверьте, открыт ли порт 80:
   ```bash
   sudo ufw status | grep 80
   ```

4. Попробуйте вручную:
   ```bash
   sudo certbot certonly --standalone -d stats.yourdomain.ru
   ```

### Статистика показывает нули

**Проблема:** Панель работает, но нет данных

**Причины:**
- База данных пуста (нет пользователей)
- Неправильный путь к базе

**Решения:**

1. Проверьте базу данных:
   ```bash
   docker compose exec creativity-bot ls -la /app/data/
   ```

2. Проверьте, есть ли пользователи в боте:
   - Отправьте `/start` боту в Telegram
   - Обновите панель

3. Проверьте логи на ошибки:
   ```bash
   docker compose logs | grep ERROR
   ```

---

## 📱 Мобильный доступ

Панель адаптирована для мобильных устройств. Просто откройте ссылку на телефоне:

```
https://stats.yourdomain.ru
```

Для удобства добавьте в закладки браузера.

---

## 🔄 Обновление

При обновлении бота веб-панель обновится автоматически:

```bash
cd ~/creativity-bot-rep
git pull origin main
docker compose down
docker compose up -d --build
```

---

## 📈 Мониторинг производительности

### Использование ресурсов

```bash
# Статистика контейнера
docker stats creativity-bot

# Логи Nginx
sudo tail -f /var/log/nginx/access.log

# Нагрузка на сервер
htop
```

### Резервное копирование

Панель не хранит данные — она только отображает информацию из базы данных бота. Бэкап базы:

```bash
# Создать бэкап
docker compose exec creativity-bot cp /app/data/database.db /app/data/backup.db

# Скачать на локальный компьютер
scp ubuntu@ВАШ_IP:~/creativity-bot-rep/data/database.db ./backup-$(date +%Y%m%d).db
```

---

## 💡 Дополнительные возможности

### Кастомизация

Если хотите изменить дизайн панели, отредактируйте `web-dashboard.js`:

```javascript
// Основной цвет (зелёный)
#3ecf8e → ваш цвет

// Фон
#0f0f0f → ваш цвет
```

### Интеграция с мониторингом

Используйте API для интеграции с системами мониторинга:

```bash
# Prometheus endpoint (можно добавить в будущем)
# Grafana dashboard (можно создать на основе API)
```

---

## 🆘 Поддержка

Если возникли проблемы:

1. Проверьте логи:
   ```bash
   docker compose logs -f
   sudo tail -f /var/log/nginx/error.log
   ```

2. Проверьте статус служб:
   ```bash
   docker compose ps
   sudo systemctl status nginx
   ```

3. Проверьте конфигурацию:
   ```bash
   cat .env
   sudo nginx -t
   ```

4. Создайте issue в репозитории или напишите автору: @kudinow

---

## 📚 Связанные документы

- [DASHBOARD.md](./DASHBOARD.md) — полная документация по веб-панели
- [DEPLOY.md](./DEPLOY.md) — развёртывание бота в облаке
- [ADMIN_COMMANDS.md](./ADMIN_COMMANDS.md) — административные команды
- [README.md](./README.md) — основная документация

---

**Удачного мониторинга! 📊✨**
