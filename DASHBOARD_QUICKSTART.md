# Быстрый старт: Веб-панель на удалённом сервере

Краткая инструкция для запуска веб-панели статистики на сервере в Яндекс.Облаке.

## ⚡️ За 5 минут

### 1. Подключитесь к серверу

```bash
ssh ubuntu@ВАШ_IP_АДРЕС
cd ~/creativity-bot-rep
```

### 2. Отредактируйте .env

```bash
nano .env
```

Добавьте эти строки:

```env
ENABLE_DASHBOARD=true
DASHBOARD_PORT=3000
DASHBOARD_PASSWORD=ваш_надёжный_пароль_123
```

Сохраните: `Ctrl+O` → `Enter` → `Ctrl+X`

### 3. Откройте порт (для прямого доступа)

```bash
sudo ufw allow 3000/tcp
```

### 4. Перезапустите бота

```bash
docker compose down
docker compose up -d
```

### 5. Откройте в браузере

```
http://ВАШ_IP_АДРЕС:3000
```

Введите пароль из шага 2.

---

## ✅ Готово!

Панель работает. Но это **небезопасно** для продакшена!

## 🔒 Сделайте безопасно (рекомендуется)

Для SSL и домена следуйте полной инструкции:

📖 **[DASHBOARD_DEPLOY.md](./DASHBOARD_DEPLOY.md)** — настройка Nginx + SSL

---

## 🆘 Проблемы?

### Панель не открывается

```bash
# Проверьте логи
docker compose logs | grep DASHBOARD

# Проверьте, что панель включена
cat .env | grep ENABLE_DASHBOARD

# Проверьте порт
sudo ufw status
```

### Не могу войти

```bash
# Проверьте пароль
cat .env | grep DASHBOARD_PASSWORD

# Перезапустите бота
docker compose restart
```

---

## 📚 Дополнительно

- [DASHBOARD_DEPLOY.md](./DASHBOARD_DEPLOY.md) — полная инструкция с Nginx и SSL
- [DASHBOARD.md](./DASHBOARD.md) — описание возможностей панели
- [DEPLOY.md](./DEPLOY.md) — развёртывание бота

**Удачи! 📊**
