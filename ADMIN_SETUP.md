# Настройка административных прав

## Как узнать свой Telegram ID

Есть несколько способов узнать свой Telegram ID:

### Способ 1: Через специального бота

1. Откройте бот [@userinfobot](https://t.me/userinfobot) в Telegram
2. Нажмите `/start`
3. Бот покажет ваш ID (число вида `123456789`)

### Способ 2: Через другой популярный бот

1. Откройте бот [@getmyid_bot](https://t.me/getmyid_bot) в Telegram
2. Нажмите `/start`
3. Бот покажет ваш ID

## Добавление администратора

### Локальный запуск

1. Откройте файл `.env` в корне проекта
2. Добавьте строку с вашим Telegram ID:

```env
BOT_TOKEN=ваш_токен_бота
TIMEZONE=Europe/Moscow
ADMIN_IDS=123456789
```

3. Если нужно добавить несколько администраторов, перечислите их через запятую:

```env
ADMIN_IDS=123456789,987654321,555666777
```

4. Перезапустите бота:

```bash
npm start
```

### Запуск через Docker

1. Отредактируйте файл `.env`:

```env
BOT_TOKEN=ваш_токен_бота
TIMEZONE=Europe/Moscow
ADMIN_IDS=123456789
```

2. Перезапустите контейнер:

```bash
docker compose down
docker compose up -d
```

### Запуск на сервере (Яндекс Облако)

1. Подключитесь к серверу:

```bash
ssh ubuntu@ВАШ_IP_АДРЕС
cd ~/creativity-bot-rep
```

2. Отредактируйте файл `.env`:

```bash
nano .env
```

3. Добавьте строку с вашим ID:

```env
ADMIN_IDS=123456789
```

4. Сохраните файл (Ctrl+O, Enter, Ctrl+X)

5. Перезапустите бота:

```bash
docker compose down
docker compose up -d
```

6. Проверьте, что бот запустился:

```bash
docker compose logs -f
```

## Проверка прав администратора

1. Откройте вашего бота в Telegram
2. Отправьте команду `/admin_stats`
3. Если всё настроено правильно, вы увидите статистику системы
4. Если права не настроены, бот ответит: "❌ У вас нет прав для выполнения этой команды."

## Список всех административных команд

### Мониторинг и аналитика
- `/admin_stats` — общая статистика системы
- `/admin_users` — список всех пользователей
- `/admin_user <telegram_id>` — детальная информация о пользователе
- `/admin_questions_stats` — статистика по вопросам

### Управление вопросами
- `/admin_add_question <текст>` — добавить новый вопрос
- `/admin_delete_question <id>` — удалить вопрос
- `/admin_edit_question <id> <текст>` — редактировать вопрос
- `/admin_list_questions` — список всех вопросов
- `/admin_search_question <текст>` — поиск вопроса

### Модерация предложений
- `/admin_pending_questions` — очередь предложенных вопросов
- `/admin_approve_question <id>` — одобрить вопрос
- `/admin_reject_question <id>` — отклонить вопрос

## Примеры использования

### Просмотр статистики

```
/admin_stats
```

Результат:
```
📊 Статистика системы

👥 Всего пользователей: 150
✅ Активные сегодня: 45
📅 Активные за неделю: 89
📆 Активные за месяц: 120

✅ Выполнено заданий: 3456
❌ Пропущено заданий: 234
📈 Процент выполнения: 93.7%

❓ Вопросов в базе: 103
💡 Предложений от пользователей: 12

🔥 Топ серий:
1. ID 123456789: 45 дней (рекорд: 45)
2. ID 987654321: 30 дней (рекорд: 35)
```

### Добавление нового вопроса

```
/admin_add_question 10 способов использовать старую футболку
```

### Просмотр предложенных вопросов

```
/admin_pending_questions
```

Результат:
```
💡 Предложенные вопросы:

ID 1 от пользователя 555666777 (16.12.2025):
"10 способов провести зимний день интересно"
/admin_approve_question 1 | /admin_reject_question 1

ID 2 от пользователя 111222333 (15.12.2025):
"10 способов сэкономить на продуктах"
/admin_approve_question 2 | /admin_reject_question 2
```

### Одобрение предложенного вопроса

```
/admin_approve_question 1
```

Результат:
```
✅ Вопрос одобрен и добавлен в базу:
"10 способов провести зимний день интересно"
```

## Безопасность

⚠️ **Важно:**
- Не публикуйте файл `.env` в открытых репозиториях
- Не передавайте свой Telegram ID посторонним
- Регулярно проверяйте список администраторов
- При компрометации ID — удалите его из `ADMIN_IDS` и перезапустите бота

## Часто задаваемые вопросы

### Как удалить администратора?

Откройте `.env`, удалите нужный ID из списка `ADMIN_IDS` и перезапустите бота.

### Можно ли временно отключить все админ-команды?

Да, просто закомментируйте или удалите строку `ADMIN_IDS` в `.env`:

```env
# ADMIN_IDS=123456789
```

### Что делать, если бот не отвечает на админ-команды?

1. Проверьте, что ваш ID добавлен в `.env`
2. Проверьте, что в ID нет пробелов и лишних символов
3. Перезапустите бота
4. Проверьте логи: `docker compose logs -f`

### Можно ли назначить разные уровни доступа?

В текущей версии все администраторы имеют одинаковые права. Для разных уровней доступа потребуется доработка кода.
